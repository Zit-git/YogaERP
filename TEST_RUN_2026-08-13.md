# Test Run - 2026-08-13

## Scope

Initial smoke test for the Aliyar Programs admin app.

## Environment

- Local URL: `http://127.0.0.1:4176/`
- Data source: configured Supabase project in `supabase-config.js`
- Browser path: in-app browser smoke testing started

## Checks Completed

- Local web server responds with `200 OK`.
- `app.js` passes JavaScript syntax validation with `node --check app.js`.
- Repository whitespace check passes with `git diff --check`.
- Working tree was clean before starting this test pass.

## Findings

### Fixed

- A developer-facing status message could still be shown near the login/logout area:
  - Previous wording: `Supabase synced - run supabase/production_schema_update.sql`
  - New wording: `Database setup needs the latest schema update`

The technical SQL file remains documented in `SUPABASE_SETUP.md`, but the app UI now avoids showing raw setup instructions to end users.

## Next Manual UAT Steps

1. Open `http://127.0.0.1:4176/`.
2. Confirm the public portal loads without login.
3. Login as Admin.
4. Refresh the page and confirm the session remains active.
5. Open the app in a second tab and confirm the login remains active.
6. Open Dashboard and verify:
   - Calendar opens on the current month.
   - Current, upcoming, and completed programs are visible.
   - Multi-day programs display continuously across dates.
   - Overlapping programs display separately.
7. Continue with Courses and Programs UAT from `TESTING.md`.
