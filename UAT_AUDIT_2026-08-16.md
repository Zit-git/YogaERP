# UAT Audit - 2026-08-16

## Scope

Workflow, functionality, usability, and security review for the Aliyar Programs application.

## Environment

- Local URL: `http://127.0.0.1:4177/`
- Vercel URL: `https://yoga-erp.vercel.app/`
- Frontend: static `index.html`, `styles.css`, `app.js`
- Backend/data: Supabase configured in `supabase-config.js`
- Test data source: current configured Supabase project

## Completed Checks

### Smoke Checks

- PASS: Local server responds at `http://127.0.0.1:4177/`.
- PASS: Vercel deployment responds at `https://yoga-erp.vercel.app/`.
- PASS: `app.js` passes JavaScript syntax validation.
- PASS: Git whitespace check passes.
- PASS: Startup loading state now has a timeout guard instead of staying on `Loading Supabase data` indefinitely.
- PASS: Browser is loading the current script after cache busting: `app.js?v=127`.
- PASS: Browser is loading the current script after login retest: `app.js?v=128`.
- PASS: Login now preserves entered fields on failure and separates credential errors from Supabase connection errors.
- PASS: Retest on `app.js?v=129` shows the correct blocker message: `Unable to reach Supabase. Please check the internet connection and try again.`

### Source-Level Workflow Coverage

- PASS: Modules exist for Portal, Dashboard, Courses, Programs, Teachers, Registrations, Participants, Accommodations, Room Allotments, Program Halls, Certificates, and Users & Roles.
- PASS: Courses and Programs have row-click detail behavior in source.
- PASS: Programs detail has a back button to records view.
- PASS: Course detail contains pricing, hierarchy, associated teachers, session plan, and programs conducted.
- PASS: Registrations are grouped by program in source.
- PASS: Room allotment source includes room availability, allot room/change room, clear, check-in, checkout, and room cleaning actions.
- PASS: Attendance source includes P/L/A marking, Mark All Present, and reason capture for Late/Absent.
- PASS: Participant visibility is filtered for participant role in source.
- PASS: Teacher visibility is filtered to assigned programs in source.

## Findings

### Critical - Supabase Policies Are Still Demo-Wide

The current sync policy file uses temporary demo policies such as:

- `using (true)`
- `with check (true)`

Impact:

- Browser UI permissions are not enough for production security.
- A user with the public anon key can potentially read/write tables directly if these policies are active.
- Participant-only and teacher-only restrictions can be bypassed outside the UI.

Recommendation:

- Replace demo-wide policies with role-aware Supabase RLS policies.
- Move privileged writes to a backend/API layer or Supabase Edge Function using service-role logic.
- Keep public access limited to upcoming program listing and guest registration only.

### Critical - Business Data Is Rendered With `innerHTML`

Many screens render user-entered values through template strings and `innerHTML`.

Impact:

- A malicious value entered into fields such as name, notes, address, course name, teacher name, or eligibility could render HTML in the app.
- This creates stored XSS risk if unsafe text is saved in Supabase and later viewed by another user.

Recommendation:

- Add HTML escaping for all user-entered values before rendering in template strings.
- Prefer DOM creation/textContent for future UI rendering.
- Test with values such as `<script>alert(1)</script>` and `<img src=x onerror=alert(1)>`.

### High - Login Depends On Full Data Load - Fixed In App

The login function currently blocks if `hasLoadedRemoteData` is false.

Impact:

- If one data table is temporarily unavailable, login cannot proceed even if Supabase Auth is working.
- This can make troubleshooting harder for admins.

Recommendation:

- Consider allowing login first, then loading role-scoped data after authentication.
- Show module-level loading/failure states instead of blocking the entire app.

Fix applied:

- Login now attempts Supabase Auth independently of full data preload.
- Failed login attempts no longer clear the email/password fields.
- Supabase Auth calls now use the same timeout wrapper.
- Supabase Auth connection errors now use friendly connection messages instead of always saying `Invalid username or password.`

### High - Supabase Is Unreachable In Current Test Browser

Current browser state after loading `http://127.0.0.1:4177/?uat=20260816-v127`:

- Active view: Portal
- Auth card: `Data connection unavailable`
- Toast: `Unable to reach Supabase. Please check the internet connection and try again.`
- Portal table: no upcoming programs because remote data did not load

Impact:

- Admin login cannot complete until the browser can reach the configured Supabase project.
- Full end-to-end workflow tests are blocked because all business data is Supabase-backed.

Recommendation:

- Confirm the Supabase project URL in `supabase-config.js` is reachable from the browser.
- Confirm the project is not paused or deleted.
- Confirm the local browser/network can resolve `jzwtgazgbcbxnzylaxbm.supabase.co`.
- Retest login after connectivity is restored.

Follow-up on 2026-08-17 after the database was resumed:

- PASS: Base Supabase endpoint responds to `https://jzwtgazgbcbxnzylaxbm.supabase.co/rest/v1/`.
- Expected response without API key: `401 UNAUTHORIZED_MISSING_API_KEY`.
- BLOCKED: The app still shows `Unable to load data from Supabase. Please refresh and try again.`
- BLOCKED: Table/API requests from the local test environment are still intermittently failing with DNS/fetch failures.

Conclusion:

- The project is no longer fully paused.
- Full workflow UAT is still blocked until table reads through the configured anon key are stable.

### Admin Login Retest - Blocked

Test credentials used:

- Email: `praveenm@gmail.com`
- Password: test password supplied by user in chat

Result on `app.js?v=128`:

- Active view remains: Portal
- Auth card remains: Public Portal
- Toast shown: `Invalid username or password.`
- Email/password fields remain populated after failed login, which confirms the login form no longer clears failed attempts.

Conclusion:

- The previous app-side blocker, `Supabase data is still loading`, is fixed.
- Admin workflow UAT is now blocked because Supabase Auth did not accept the supplied test credential.

Follow-up result on `app.js?v=129`:

- The app now reports `Unable to reach Supabase. Please check the internet connection and try again.`
- This confirms the failed login was a Supabase connectivity/auth-service reachability issue, not necessarily a wrong test credential.

Follow-up result on Vercel, 2026-08-17:

- PASS: Vercel app loaded successfully.
- PASS: Admin login succeeded with the supplied test credential.
- PASS: App opened the Dashboard after login.
- PASS: Left navigation was visible for Admin:
  - Dashboard
  - Programs
  - Registrations
  - Room Allotments
  - Certificates
  - Courses
  - Teachers
  - Participants
  - Accommodations
  - Program Halls
  - Users & Roles
- WARNING: Auth card still shows `Database setup needs the latest schema update`.
- FIXED: Console showed `Cannot read properties of null (reading 'reset')` after login because submit handlers used `event.currentTarget` after async work. Submit handlers now capture a stable form reference before awaiting.

### Vercel Read-Only Navigation Pass - 2026-08-17

Starting state:

- PASS: Auth card shows `Supabase connected`.
- PASS: Dashboard opens to `August 2026`.
- PASS: Calendar renders 42 day cells.
- PASS: No console errors during the read-only navigation pass.
- NOTE: Deployed Vercel build was still loading `app.js?v=129`; local fix has advanced to `app.js?v=130`.

Navigation results:

- PASS: Dashboard opens `Operations Dashboard`.
- PASS: Programs opens `Program Management` records view.
- PASS: Registrations opens `Registration & Eligibility` records view grouped by program.
- PASS: Room Allotments opens `Room Allotments` with available room list and allocation controls.
- PASS: Certificates opens `Completion & Certificates`.
- PASS: Courses opens `Course Master` records view.
- PASS: Teachers opens `Teacher Master` records view.
- PASS: Participants opens `Participants Master` records view.
- PASS: Accommodations opens `Accommodation Management` records view.
- PASS: Program Halls opens `Program Halls` records view.
- PASS: Users & Roles opens `Users & Roles` records view.

Observed data state:

- Programs default `Active Programs` filter shows no rows.
- Registrations show grouped records with sample/past program data.
- Teachers, Participants, Accommodation, Program Halls, Users, and Roles all show records.

Usability notes:

- The default Programs view may look empty when there are no active programs. The `All Programs` filter is available, but this should be verified visually with users to ensure it is not confusing.
- The warning `Database setup needs the latest schema update` is still visible on the deployed app and should be resolved by running the latest schema update or hiding technical status from normal users.

### High - Supabase Anon Key Is Public, So RLS Must Be Production-Grade

The anon key in `supabase-config.js` is expected for a browser app, but it means database security must be enforced in Supabase, not only in JavaScript.

Impact:

- This is acceptable only if RLS policies are strict.
- With demo-wide policies, this is not production safe.

Recommendation:

- Do not deploy with `temporary_demo_*` policies.
- Add a production RLS script and keep the demo script clearly separated.

### Medium - Browser-Control Output Is Currently Not Returning Readable Snapshots

The in-app browser controller connects, but text snapshots are not being returned to the chat in this session.

Impact:

- Automated visual verification from this chat is limited.
- Manual confirmation in the open browser is still needed for login/password workflows.

Recommendation:

- Continue source-level audit and manual UAT together.
- User should enter credentials directly in the browser; do not share passwords in chat.

## Blocked / Needs User Action

- Admin login workflow: requires the user to enter admin credentials in the browser.
- Admin login workflow: currently blocked by Supabase connectivity failure in the browser.
- Supabase Table Editor verification: requires access to the Supabase console or user confirmation after each save/edit/delete.
- End-to-end data creation tests: should be done only after confirming this is a test/staging Supabase project or using disposable test records.

## Next Test Pass

1. User logs in as Admin in the browser.
2. Verify Dashboard current/upcoming/completed calendar visually.
3. Create disposable test records:
   - Course
   - Teacher
   - Program
   - Registration
   - Participant
   - Room allotment
4. Verify each record appears in Supabase.
5. Test role access:
   - Admin
   - Teacher
   - Participant
6. Fix critical security items before production use.
